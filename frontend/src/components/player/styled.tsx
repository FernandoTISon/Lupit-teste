'use client'
import styled from 'styled-components'

export const PlayerContainer = styled.section`
    display: flex;
    flex-direction: column;
    gap: 10px;
`

export const PlayerTitle = styled.article`

`
export const CardsSection = styled.div `
    width: 100vw;
    height: 20vh;
    display: flex;
    flex-direction: row;
    gap: 16px;
`

